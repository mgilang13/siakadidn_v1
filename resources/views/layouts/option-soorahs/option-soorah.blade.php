<optgroup class="bg-secondary text-light" label="Juz 30">
@foreach ($soorahs as $soorah)
    @if($soorah->order <= 114 and $soorah->order >= 78)
        <option value="{{ $soorah->id }}">{{ $soorah->order }}. {{ $soorah->name }}</option>
    @endif
@endforeach
</optgroup>

<optgroup class="bg-secondary text-light" label="Juz 29">
@foreach ($soorahs as $soorah)
    @if($soorah->order <= 77 and $soorah->order >= 67)
        <option value="{{ $soorah->id }}">{{ $soorah->order }}. {{ $soorah->name }}</option>
    @endif
@endforeach
</optgroup>

<optgroup class="bg-secondary text-light" label="Juz 28">
@foreach ($soorahs as $soorah)
    @if($soorah->order <= 66 and $soorah->order >= 58)
        <option value="{{ $soorah->id }}">{{ $soorah->order }}. {{ $soorah->name }}</option>
    @endif
@endforeach
</optgroup>

<optgroup class="bg-secondary text-light" label="Juz 27">
@foreach ($soorahs as $soorah)
    @if($soorah->order <= 57 and $soorah->order >= 51)
        <option value="{{ $soorah->id }}">{{ $soorah->order }}. {{ $soorah->name }}</option>
    @endif
@endforeach
</optgroup>

<optgroup class="bg-secondary text-light" label="Juz 26">
@foreach ($soorahs as $soorah)
    @if($soorah->order <= 51 and $soorah->order >= 46)
        <option value="{{ $soorah->id }}">{{ $soorah->order }}. {{ $soorah->name }}</option>
    @endif
@endforeach
</optgroup>

<optgroup class="bg-secondary text-light" label="Juz 25">
@foreach ($soorahs as $soorah)
    @if($soorah->order <= 45 and $soorah->order >= 41)
        <option value="{{ $soorah->id }}">{{ $soorah->order }}. {{ $soorah->name }}</option>
    @endif
@endforeach
</optgroup>

<optgroup class="bg-secondary text-light" label="Juz 24">
@foreach ($soorahs as $soorah)
    @if($soorah->order <= 41 and $soorah->order >= 39)
        <option value="{{ $soorah->id }}">{{ $soorah->order }}. {{ $soorah->name }}</option>
    @endif
@endforeach
</optgroup>
<optgroup class="bg-secondary text-light" label="Juz 23">
@foreach ($soorahs as $soorah)
    @if($soorah->order <= 39 and $soorah->order >= 36)
        <option value="{{ $soorah->id }}">{{ $soorah->order }}. {{ $soorah->name }}</option>
    @endif
@endforeach
</optgroup>

<optgroup class="bg-secondary text-light" label="Juz 22">
@foreach ($soorahs as $soorah)
    @if($soorah->order <= 36 and $soorah->order >= 33)
        <option value="{{ $soorah->id }}">{{ $soorah->order }}. {{ $soorah->name }}</option>
    @endif
@endforeach
</optgroup>

<optgroup class="bg-secondary text-light" label="Juz 21">
@foreach ($soorahs as $soorah)
    @if($soorah->order <= 33 and $soorah->order >= 29)
        <option value="{{ $soorah->id }}">{{ $soorah->order }}. {{ $soorah->name }}</option>
    @endif
@endforeach
</optgroup>

<optgroup class="bg-secondary text-light" label="Juz 20">
@foreach ($soorahs as $soorah)
    @if($soorah->order <= 29 and $soorah->order >= 27)
        <option value="{{ $soorah->id }}">{{ $soorah->order }}. {{ $soorah->name }}</option>
    @endif
@endforeach
</optgroup>

<optgroup class="bg-secondary text-light" label="Juz 19">
@foreach ($soorahs as $soorah)
    @if($soorah->order <= 27 and $soorah->order >= 25)
        <option value="{{ $soorah->id }}">{{ $soorah->order }}. {{ $soorah->name }}</option>
    @endif
@endforeach
</optgroup>
<optgroup class="bg-secondary text-light" label="Juz 18">
@foreach ($soorahs as $soorah)
    @if($soorah->order <= 25 and $soorah->order >= 23)
        <option value="{{ $soorah->id }}">{{ $soorah->order }}. {{ $soorah->name }}</option>
    @endif
@endforeach
</optgroup>
<optgroup class="bg-secondary text-light" label="Juz 17">
@foreach ($soorahs as $soorah)
    @if($soorah->order <= 22 and $soorah->order >= 21)
        <option value="{{ $soorah->id }}">{{ $soorah->order }}. {{ $soorah->name }}</option>
    @endif
@endforeach
</optgroup>
<optgroup class="bg-secondary text-light" label="Juz 16">
@foreach ($soorahs as $soorah)
    @if($soorah->order <= 20 and $soorah->order >= 18)
        <option value="{{ $soorah->id }}">{{ $soorah->order }}. {{ $soorah->name }}</option>
    @endif
@endforeach
</optgroup>
<optgroup class="bg-secondary text-light" label="Juz 15">
@foreach ($soorahs as $soorah)
    @if($soorah->order <= 18 and $soorah->order >= 17)
        <option value="{{ $soorah->id }}">{{ $soorah->order }}. {{ $soorah->name }}</option>
    @endif
@endforeach
</optgroup>
<optgroup class="bg-secondary text-light" label="Juz 14">
@foreach ($soorahs as $soorah)
    @if($soorah->order <= 16 and $soorah->order >= 15)
        <option value="{{ $soorah->id }}">{{ $soorah->order }}. {{ $soorah->name }}</option>
    @endif
@endforeach
</optgroup>
<optgroup class="bg-secondary text-light" label="Juz 13">
@foreach ($soorahs as $soorah)
    @if($soorah->order <= 14 and $soorah->order >= 12)
        <option value="{{ $soorah->id }}">{{ $soorah->order }}. {{ $soorah->name }}</option>
    @endif
@endforeach
</optgroup>
<optgroup class="bg-secondary text-light" label="Juz 12">
@foreach ($soorahs as $soorah)
    @if($soorah->order <= 12 and $soorah->order >= 11)
        <option value="{{ $soorah->id }}">{{ $soorah->order }}. {{ $soorah->name }}</option>
    @endif
@endforeach
</optgroup>
<optgroup class="bg-secondary text-light" label="Juz 11">
@foreach ($soorahs as $soorah)
    @if($soorah->order <= 11 and $soorah->order >= 9)
        <option value="{{ $soorah->id }}">{{ $soorah->order }}. {{ $soorah->name }}</option>
    @endif
@endforeach
</optgroup>
<optgroup class="bg-secondary text-light" label="Juz 10">
@foreach ($soorahs as $soorah)
    @if($soorah->order <= 9 and $soorah->order >= 8)
        <option value="{{ $soorah->id }}">{{ $soorah->order }}. {{ $soorah->name }}</option>
    @endif
@endforeach
</optgroup>
<optgroup class="bg-secondary text-light" label="Juz 9">
@foreach ($soorahs as $soorah)
    @if($soorah->order <= 8 and $soorah->order >= 7)
        <option value="{{ $soorah->id }}">{{ $soorah->order }}. {{ $soorah->name }}</option>
    @endif
@endforeach
</optgroup>
<optgroup class="bg-secondary text-light" label="Juz 8">
@foreach ($soorahs as $soorah)
    @if($soorah->order <= 7 and $soorah->order >= 6)
        <option value="{{ $soorah->id }}">{{ $soorah->order }}. {{ $soorah->name }}</option>
    @endif
@endforeach
</optgroup>
<optgroup class="bg-secondary text-light" label="Juz 7">
@foreach ($soorahs as $soorah)
    @if($soorah->order <= 6 and $soorah->order >= 5)
        <option value="{{ $soorah->id }}">{{ $soorah->order }}. {{ $soorah->name }}</option>
    @endif
@endforeach
</optgroup>
<optgroup class="bg-secondary text-light" label="Juz 6">
@foreach ($soorahs as $soorah)
    @if($soorah->order <= 5 and $soorah->order >= 4)
        <option value="{{ $soorah->id }}">{{ $soorah->order }}. {{ $soorah->name }}</option>
    @endif
@endforeach
</optgroup>
<optgroup class="bg-secondary text-light" label="Juz 5">
@foreach ($soorahs as $soorah)
    @if($soorah->order <= 4 and $soorah->order >= 4)
        <option value="{{ $soorah->id }}">{{ $soorah->order }}. {{ $soorah->name }}</option>
    @endif
@endforeach
</optgroup>
<optgroup class="bg-secondary text-light" label="Juz 4">
@foreach ($soorahs as $soorah)
    @if($soorah->order <= 4 and $soorah->order >= 3)
        <option value="{{ $soorah->id }}">{{ $soorah->order }}. {{ $soorah->name }}</option>
    @endif
@endforeach
</optgroup>
<optgroup class="bg-secondary text-light" label="Juz 3">
@foreach ($soorahs as $soorah)
    @if($soorah->order <= 3 and $soorah->order >= 2)
        <option value="{{ $soorah->id }}">{{ $soorah->order }}. {{ $soorah->name }}</option>
    @endif
@endforeach
</optgroup>
<optgroup class="bg-secondary text-light" label="Juz 2">
@foreach ($soorahs as $soorah)
    @if($soorah->order <= 2 and $soorah->order >= 2)
        <option value="{{ $soorah->id }}">{{ $soorah->order }}. {{ $soorah->name }}</option>
    @endif
@endforeach
</optgroup>
<optgroup class="bg-secondary text-light" label="Juz 1">
@foreach ($soorahs as $soorah)
    @if($soorah->order <= 2 and $soorah->order >= 1)
        <option value="{{ $soorah->id }}">{{ $soorah->order }}. {{ $soorah->name }}</option>
    @endif
@endforeach
</optgroup>